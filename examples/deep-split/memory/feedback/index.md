↑ ../index.md

# Feedback

One entry per corrected or confirmed approach, each with `**Why:**` and `**How to apply:**` —
this list itself outgrew a single file, so each entry now lives in its own file, one layer down.

- [No mocked datastore in integration tests](no-mock-datastore.md) — a mocked test passed while
  the real migration broke

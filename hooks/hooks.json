{
  "description": "cimble: warn/block when an edited CLAUDE.md or memory/*.md file crosses its growth threshold (120 lines/file, 40 lines/section by default).",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/check_memory_hook.py\"",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
